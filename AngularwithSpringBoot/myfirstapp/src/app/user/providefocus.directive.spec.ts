import { ProvidefocusDirective } from './providefocus.directive';

describe('ProvidefocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ProvidefocusDirective();
    expect(directive).toBeTruthy();
  });
});
